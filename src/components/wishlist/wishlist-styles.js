import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    inputs: {
        paddingLeft: 16,
        paddingRight: 16,
        flex: 1
    },

    inputLabelContainer: {
        flexDirection: 'row',
        alignItems: 'flex-end'
    },

    section: {
        marginTop: 15,
        marginBottom: 10
    }
});